{
  "collectionName": "components_shop_variants",
  "info": {
    "displayName": "Variant",
    "description": "Variante produit : coloris, taille A/D, prix, EAN, stock, images"
  },
  "options": {},
  "attributes": {
    "sku": { "type": "string" },
    "color_code": { "type": "string", "required": true },
    "color_label": { "type": "string" },

    "size_a": { "type": "integer", "min": 0 },
    "size_d": { "type": "integer", "min": 0 },

    "ean": { "type": "string" },
    "stock": { "type": "integer" },

    "status": {
      "type": "enumeration",
      "enum": ["active", "draft", "archived"],
      "default": "active"
    },

    "price_ht": { "type": "decimal" },

   
    "image_urls": { "type": "json" }

    
  }
}
